<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiSample.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Image
                Source="dotnet_bot.png"
                HeightRequest="185"
                Aspect="AspectFit"
                SemanticProperties.Description="dot net bot in a race car number eight" />

            <Label
                x:Name="newBindingSampleLabel"
                Text="Hello, World!"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Label
                Text="Trigger TikTok App Events"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2" />

            <Button
                Text="Login Event"
                x:Name="LoginEvtBtn"
                Clicked="LoginEvtBtn_OnClicked"
                HorizontalOptions="Fill" />

            <Button
                Text="Registration Event"
                x:Name="RegEvtBtn"
                Clicked="RegEvtBtn_OnClicked"
                HorizontalOptions="Fill" />

            <Button
                Text="Subscribe Event"
                x:Name="SubEvtBtn"
                Clicked="SubEvtBtn_OnClicked"
                HorizontalOptions="Fill" />

            <Button
                Text="Spend Credits Event"
                x:Name="SpendCreditsEvtBtn"
                Clicked="SpendCreditsEvtBtn_OnClicked"
                HorizontalOptions="Fill" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
